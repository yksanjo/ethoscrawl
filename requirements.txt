# Core dependencies
httpx>=0.24.0
beautifulsoup4>=4.12.0
lxml>=4.9.0
pydantic>=2.0.0
python-dateutil>=2.8.0
cryptography>=41.0.0
aiofiles>=23.0.0

# Optional dependencies (install with pip install ethoscrawl[ai])
# openai>=1.0.0
# anthropic>=0.7.0

# Optional dependencies (install with pip install ethoscrawl[scrapling])
# scrapling>=0.1.0